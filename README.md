# Pregnant Employees Backend

The active implementation is the Node.js, Express, and Supabase API in [`backend/`](backend/README.md). It replaces the previously tracked Spring Boot/MySQL prototype and does not contain a frontend.

See the backend README for setup, security notes, endpoint documentation, and Postman examples.
